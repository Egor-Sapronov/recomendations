import React from 'react';

export default ({content}) => {
    return (
        <div
            className="mdl-typography--display-1"
            style={{fontWeight: '200'}}
        >{content}</div>
    );
};
